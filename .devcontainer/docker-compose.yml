services:
  vscode:
    image: latex-template
    container_name: latex-template_dc
    build: .
    volumes:
      - ../:/workspace
      - ~/.ssh:/home/vscode/.ssh
      - ~/.gitconfig:/home/vscode/.gitconfig
    entrypoint: ["zsh", "-c", "while sleep 1000; do :; done"]
